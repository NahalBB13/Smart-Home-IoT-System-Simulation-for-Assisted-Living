package iot;
//This all-in-one home security system captures video and audio and sends alerts to a user’s smartphone.
//It automatically knows when they are home or away (no need to enter a security code), 
//and a user can also view the live video feed from their phone.

simple SecurityCamera
{
    gates:
        inout gate[];
}
